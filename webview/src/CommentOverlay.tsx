// CommentOverlay is no longer used — pins replaced by element-anchored sidebar model.
// Retained as an empty stub to avoid import errors during migration.
import React from 'react';

export function CommentOverlay(): React.ReactElement {
  return <></>;
}
